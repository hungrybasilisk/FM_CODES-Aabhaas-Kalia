#include <stdio.h>
int main()
{
    //printing the output using printf
    printf("*\n**\n***\n****\n*****\n");
    return 0;
}
/* We can also use the the code from Question three and modify n to the numer of rows you want 
#include <stdio.h>

int main() {
    int n;

    printf("Enter the number of rows: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++)
        {
        for (int j = 1; j <= i; j++)
         {
            printf("* ");
        }

        // Move to the next line after each row
        printf("\n");
    }

    return 0;
}

*/
